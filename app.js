const form=document.querySelector('#searchForm');const out=document.querySelector('#resultBody');form.addEventListener('reset',()=>{out.innerHTML=''});form.addEventListener('submit',event=>{event.preventDefault();const registro=document.querySelector('#registro').value.trim();const nome=document.querySelector('#nome').value.trim();const documento=document.querySelector('#documento').value.trim();if(!registro&&!nome&&!documento){out.innerHTML='<p class="message">Informe ao menos um filtro para realizar a consulta demonstrativa.</p>';return}if(documento){out.innerHTML='<p class="message">Nenhum dado foi enviado. Esta demonstração não consulta nem armazena CPF/CNPJ.</p>';return}out.innerHTML='<div class="result-card"><strong>REGISTRO DEMONSTRATIVO</strong><span>BIOMÉDICO(A)</span><span>Nº 00001</span></div>'});
